<%- editor = user_context["editor"].to_s -%>
<%- shell  = user_context["login_shell"].to_s -%>
**Editor:** <%= editor == "code" ? "VS Code" : "JupyterLab" %>
<%- unless shell.empty? -%>
 &middot; **Shell:** `<%= File.basename(shell) %>`
<%- end -%>

The introduction opens with the session — each lesson as a page or a notebook;
`cirrus-intro` lists them. Run `cirrus-check` in a terminal to confirm cluster
access.
